apiVersion: v1
kind: Service
metadata:
  name: {{ include "buildaosp.fullname" . }}
  labels:
    {{- include "buildaosp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "buildaosp.selectorLabels" . | nindent 4 }}
