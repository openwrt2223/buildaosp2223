apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "buildaosp.fullname" . }}-test-connection"
  labels:
    {{- include "buildaosp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "buildaosp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
